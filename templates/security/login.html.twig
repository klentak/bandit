{% extends 'base.html.twig' %}

{% block title %}Strona logowania{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <div class="container">
    <div class="form-group">
    <h1>Strona logowania</h1>
    <form action="{{ path('login') }}" method="post">
        <input class="form-control" type="text" name="_username" value="{{ lastUsername }}" placeholder="Login...">
        <input class="form-control mt-3" id="exampleInputPassword1" type="password" name="_password" placeholder="Hasło...">

        {% if error %}
            <div class="invalid-feedback">{{ error.messageKey }}</div>
        {% endif %}

        <button class="btn btn-primary mt-3" type="submit">Zaloguj</button>
    </form>
        <a href="register"><button type="submit" class="btn btn-success mt-3">rejestracja</button></a>

    </div>
{% endblock %}
